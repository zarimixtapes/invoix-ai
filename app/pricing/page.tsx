import Link from 'next/link';
const plans=[['Solo','49','For one supervisor validating the workflow',['Voice-to-diary','Delay and variation drafts','PDF/report exports','Basic project memory']],['Company','199','For builders running multiple jobs',['Everything in Solo','Teams and permissions','Client/HQ/architect email drafts','Google Drive sync','Safety, defects and timesheets']],['Scale','499','For growing contractors that need control',['Everything in Company','Multiple workspaces','Advanced audit log','Priority support','Custom templates']]];
export default function Pricing(){return <main className="container"><nav className="nav"><Link className="logo" href="/">BuildMind <span>AI</span></Link><Link className="btn secondary" href="/dashboard">Demo</Link></nav><section className="section"><div className="eyebrow">14-day trial included</div><h1 style={{fontSize:58,letterSpacing:'-.07em',margin:'12px 0'}}>Pricing built for small builders first.</h1><p className="lead">Start with the AI documentation wedge, then expand into project memory, reports, safety, defects and team controls.</p><div className="pricing">{plans.map((p,i)=><div className="card" key={p[0]}><span className={`badge ${i===1?'ok':''}`}>{i===1?'Recommended':'Plan'}</span><h2>{p[0]}</h2><div className="price">${p[1]}<span className="muted" style={{fontSize:16}}>/mo</span></div><p className="muted">{p[2]}</p><div className="timeline">{(p[3] as string[]).map(x=><div className="event" key={x}>{x}</div>)}</div><Link className="btn" style={{width:'100%',marginTop:14}} href="/signup">Start free trial</Link></div>)}</div></section></main>}
