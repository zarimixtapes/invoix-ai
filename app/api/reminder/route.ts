import{NextResponse}from'next/server';export async function POST(req:Request){const{customerName,invoiceNumber,amount,dueDate,tone='polite'}=await req.json();const fallback=`Hi ${customerName}, just a friendly reminder that invoice ${invoiceNumber} for $${amount} was due on ${dueDate}. Please let me know if you have any questions. Thank you.`;if(!process.env.OPENAI_API_KEY)return NextResponse.json({message:fallback});try{const prompt=`Write a short ${tone} payment reminder for ${customerName}. Invoice ${invoiceNumber}. Amount ${amount}. Due ${dueDate}. Friendly and professional.`;const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-4.1-mini',input:prompt,temperature:.4})});const d=await r.json();return NextResponse.json({message:d.output_text||fallback})}catch{return NextResponse.json({message:fallback})}}
